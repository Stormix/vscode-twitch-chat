import * as vscode from 'vscode';

export const getTwitchSettings = () => {
  // const gender = vscode.workspace.getConfiguration('webviewReact').get('userApiGender', 'male');
  // return gender;
  return {};
};
